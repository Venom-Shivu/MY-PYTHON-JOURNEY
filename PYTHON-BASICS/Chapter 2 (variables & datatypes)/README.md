
# Chapter 2: Variables and Data Types

![Banner](https://img.shields.io/badge/Chapter_2-Variables_&_Data_Types-blue?style=for-the-badge&logo=python&logoColor=white)

## 📌 Overview
This chapter explains how Python stores data using **variables** and how different **data types** define the kind of values a variable can hold.  
Python uses **dynamic typing**, so variable types are assigned at runtime.

---

## 📚 Topics Covered

### 1. Variables
Variables act as named references to data stored in memory.

**Rules for naming variables:**
- Must start with a letter or underscore (`_`)
- Cannot start with a number
- Case-sensitive (`age` ≠ `Age`)

---

### 2. Data Types

Python provides built-in data types to represent different kinds of data.

* **int** – Whole numbers (`10`, `-5`)
* **float** – Decimal values (`3.14`, `-0.01`)
* **str** – Text data (`"Hello"`)
* **bool** – Logical values (`True`, `False`)
* **NoneType** – Represents no value (`None`)

---

### 3. Type Casting

Type casting is the process of converting one data type into another.

---

## ✅ Key Takeaways

* Variables store data dynamically
* Data types define how data behaves
* Type casting enables controlled data conversion

```

