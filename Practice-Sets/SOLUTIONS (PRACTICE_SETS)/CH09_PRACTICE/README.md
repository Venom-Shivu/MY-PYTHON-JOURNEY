# 📘 Chapter 9: File I/O - Practice Set

## 📌 Overview
This folder contains solutions to the practice problems for **Chapter 9**.
These exercises focus on reading from and writing to files, which is essential for data persistence.

## 📂 Key Concepts Covered
- **File Modes**: Read (`r`), Write (`w`), Append (`a`), and Update (`+`).
- **Reading Files**: Using `read()`, `readline()`, and `readlines()`.
- **Writing Files**: Creating and modifying text files.
- **Context Manager**: Using the `with` statement for safe file handling.
- **Binary Files**: Basic handling of non-text files.

## 🚀 How to Run
Navigate to this directory and run the solution files:
```bash
python Problem_01.py
```