# To Check the type of the variable assigned using input() function
a = input ("Enter the value of a")
# We have to always keepn in mind that the type of input is always string
#whatever will be the input we get the result as string.
# We can convert that later using type conversion 
print(type(a))